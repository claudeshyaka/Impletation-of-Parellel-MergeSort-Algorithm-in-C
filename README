### parallel implementation of merge sort.

### Files included:
main.cpp: the main routine that will invoke the two different implementations 
  of sort and check their results;
ktiming.cpp/.h: code for time-measurement;
cilk_sort.cpp: where the Cilk Plus mergesort implementation should go;
pthread_sort.cpp: where the pthreaded mergesort implementation should go; 
qsub.sh: example script for job submittion; and
Makefile

